# Problembrief — 2026-09-21

## TL;DR

Scanned 109 items and found 2 new, non-duplicate issues. Strongest signal: **Change Data Capture (CDC) systems only copy rows unchanged, preventing necessary data transformations, aggregations, or integration with target database logic during replication.**

## New Issues

### Change Data Capture (CDC) systems only copy rows unchanged, preventing necessary data transformations, aggregations, or integration with target database logic during replication

**Category:** Devtools
**Problem:** Change Data Capture (CDC) systems only copy rows unchanged, preventing necessary data transformations, aggregations, or integration with target database logic during replication.
**Evidence:** What exists today moves a row **unchanged**. The case this does not serve: a row appears in a table in database X, and I want a row of **my own shape** in database Y — computed from it, and written so that Y's own machinery runs. There is nowhere to say `total = qty * unit_price`, `status = case ... end`, or "one source row becomes two target rows", let alone "look something up in Y first". an `identity` / `serial` / sequence on the target never fires — the source's key is inserted verbatim; a column `default` never applies, for the same reason; `before insert` / `after insert` triggers do fire for the insert itself, but they s
**Current workaround:** Implementing custom scripting or separate ETL processes outside the CDC system.
**Manual steps:** Writing custom transformation logic, Managing separate ETL jobs
**Who feels it:** Data Engineer
**Likely buyer:** Data Engineering Manager
**Scores:** pain 5 · freq 3 · build 3 · market 4 · interest 5
**Sources:** [GitHub](https://github.com/gkhnelbstn/lightweight-data-platform/issues/45)

### Deploying and utilizing large language models with MoE architectures is challenging due to their significant and specific hardware resource demands (VRAM, RAM), often exceeding typical user capabilities

**Category:** Ai Agents
**Problem:** Deploying and utilizing large language models with MoE architectures is challenging due to their significant and specific hardware resource demands (VRAM, RAM), often exceeding typical user capabilities.
**Evidence:** Any idea of what the minimum GPU VRAM size should be given the 6B active params? ... minimum GPU VRAM size NVFP4: 16 GB can barely boot (tiny KV cache); 24 GB serves the full 262k context. More VRAM = larger expert cache > offloading the 51B N-gram on disk Not in this first PR — the table sits in pinned host RAM (~111 GiB total incl. the expert banks, >= 128 GB recommended).
**Current workaround:** Users attempt to estimate hardware needs, often finding their systems insufficient, or wait for model providers to implement specific optimizations like disk offloading.
**Manual steps:** Estimating hardware requirements, Configuring systems for optimal performance, Dealing with out-of-memory errors
**Who feels it:** AI Developer, ML Engineer, Researcher
**Likely buyer:** Engineering Manager, Head of AI/ML
**Scores:** pain 4 · freq 3 · build 3 · market 4 · interest 5
**Sources:** [GitHub](https://github.com/FlashML-org/FreeToken/issues/214)

Generated by Problembrief.
